// The site's one blue accent — mirrors --accent-blue / --ring in
// src/styles/global.css. Kept as a JS constant too since shader `colors`
// arrays (DitheringBackground, GradientBackground) take plain hex strings
// and can't reference a CSS custom property.
export const ACCENT_BLUE = '#38bdf8';
